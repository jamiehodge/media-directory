json = collection.map {|item|
  {
    id:           item.id,
    url:          url(item.id),
    name:         item.name,
    description:  item.description,
    department:   item.department,
    organization: item.room,
    telephone:    item.telephone,
    mail:         item.mail
  }
}
